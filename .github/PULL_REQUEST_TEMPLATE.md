**What does this change and why?**


**How was it tested?**
(e.g. throwaway harness against a temp folder, `dotnet run`, `build.ps1` output)


**Checklist**
- [ ] Tested against a throwaway folder, not a real Steam screenshot library
- [ ] `app/*.cs` / `*.ps1` changes are pure ASCII (see CONTRIBUTING.md)
- [ ] Version bumped in both `app/SteamScreenshotBackup.csproj` and
      `installer/setup.iss`, if this changes released behavior
